import 'package:flutter/material.dart';

class PopularFriendWidget extends StatelessWidget {
  const PopularFriendWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
